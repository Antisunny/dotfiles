function git-c-gh() {
    # 个人github
    git config user.name "Ryan Lincs"
    git config user.email "ryan.lincs@outlook.com"
}
function git-c-w() {
    # 工作环境
    git config user.name "Rili"
    # git config user.email "ryan.lincs@outlook.com"
}

function git-c-p() {
    # 其他个人部署的git
    git config user.name "Rili"
    # git config user.email "ryan.lincs@outlook.com"
}